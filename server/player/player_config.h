#pragma once

#include "common/physics/physics_config.h"

namespace PlayerConfig {
static constexpr int full_health = 100;
static constexpr int initial_money = 800;
static constexpr int hitbox_radius = PhysicsConfig::meter_size / 3;
};  // namespace PlayerConfig
