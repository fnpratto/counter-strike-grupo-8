#pragma once

enum class Team { Terrorist, CounterTerrorist };
enum class MoveDirection { Up, Down, Left, Right };
enum class GunType { AK47, M3, AWP, Glock };
enum class UtilityType { Knife, C4 };
enum class WeaponSlot { Primary, Secondary, Melee, Bomb };
enum class PhaseType { NotStarted, Buying, Playing, RoundFinished };
enum class MapTileType { Floor, Wall, Box };
