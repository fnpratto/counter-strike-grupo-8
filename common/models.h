#pragma once

enum class Team : uint8_t { Terrorist, CounterTerrorist };
enum class MoveDirection { Up, Down, Left, Right };
enum class WeaponType { Glock, AK47, M3, AWP };
enum class WeaponSlot { Primary, Secondary, Knife, Bomb };
